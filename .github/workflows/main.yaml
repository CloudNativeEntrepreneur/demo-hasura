name: main
on:
  push:
    branches:
    - main
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
      with:
        submodules: recursive
    - name: Set up QEMU
      uses: docker/setup-qemu-action@v1
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1
    - name: Login to DockerHub
      uses: docker/login-action@v1 
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
    - name: Build and push container image
      uses: docker/build-push-action@v2
      with:
        push: true
        tags: |
          ${{ secrets.DOCKERHUB_USERNAME }}/demo-hasura:latest
          ${{ secrets.DOCKERHUB_USERNAME }}/demo-hasura:${{ github.sha }}
    - name: Checkout prod
      uses: actions/checkout@v2
      with:
        repository: ${{ secrets.GH_ORG }}/argocd-gitops-local-dev
        persist-credentials: false
        fetch-depth: 0
    - name: Update manifests
      run: |
        sleep 1
        cat apps/demo-hasura/overlays/staging/kustomization.yaml | sed -e "s@- ${{ secrets.DOCKERHUB_USERNAME }}/demo-hasura:.*@- ${{ secrets.DOCKERHUB_USERNAME }}/demo-hasura:${{ github.sha }}@g" | tee apps/demo-hasura/overlays/staging/kustomization.yaml
        git config --global user.email "${{ secrets.GH_ORG }}@cloudnativeentrepreneur.com"
        git config --global user.name "${{ secrets.GH_ORG }}"
        git add .
        git commit -m "Promoting demo-hasura ${{ github.sha }}"
    - name: Push prod
      uses: ad-m/github-push-action@master
      with:
        github_token: ${{ secrets.GH_TOKEN }}
        repository: ${{ secrets.GH_ORG }}/argocd-gitops-local-dev
 